func two(s string) {
    cmd := exec.Command(s)
    stdout, err := cmd.StdoutPipe()
    if err != nil {
        log.Fatal(err, "1")
    }
    if err := cmd.Start(); err != nil {
        log.Fatal(err, " 标准错误输出")
    }
    
    if err := cmd.Wait(); err != nil {
        log.Fatal(err, "4")
    }
}
